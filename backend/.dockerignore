# Secrets — provided at runtime via compose `environment:`, never baked in.
.env
.env.*
!.env.example

# Virtualenv / caches / build junk
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# Not needed at runtime
tests/
*.md
Dockerfile
.dockerignore
